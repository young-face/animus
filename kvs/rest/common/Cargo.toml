[package]
name = "animus-kvs-rest-common"
version = "0.1.0"
edition = "2024"

[dependencies]
# Internal
kvs-api.workspace = true

# External
base64.workspace = true
serde = { workspace = true, features = ["derive"] }
validator = { workspace = true, features = ["derive"] }
